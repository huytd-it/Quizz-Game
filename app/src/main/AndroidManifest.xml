<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yukihuy.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_next"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.AccountManagerActivity"/>
        <activity android:name=".View.CreditStoreActivity"></activity>
        <activity android:name=".View.RankActivity" />
        <activity android:name=".View.PlayGameHistoryActivity" />
        <activity android:name=".View.RegisterActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".View.GetPassWordActivity" />
        <activity android:name=".View.GameAdminActivity" />
        <activity android:name=".View.LinhVucActivity" />
        <activity android:name=".View.LoginActivity" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>